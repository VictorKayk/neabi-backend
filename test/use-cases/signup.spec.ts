type SutTypes = {
  sut: ,
}.

const makeSut = (): SutTypes => {
  const sut = new ();
  return {
    sut,
  };
};

describe('SignUp Use Case', () => {
  it('', () => {
    const { sut } = makeSut();
    ;
  });
});
